<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Home</title>
    <link rel="stylesheet" href="/styles.css">
    <script defer src="/script.js"></script>
</head>
<body>
    <div class="spinner-container" id="spinner-container">
        <div class="spinner"></div>
    </div>
    <div class="container">
        <h1>Welcome to the Customer Relationship Management App</h1>
            <% if (session.user) { %>
            <p>Logged in as: <%= session.user.username %></p>
            <a href="/logout">Logout</a>
        <% } else { %>
            <a href="/login">Login</a>
        <% } %>



	<a href="/list-people">List People</a><br>
	<br>
	<a href="/list-organizations">List Organizations</a><br>
	<br>
        <a href="/add-person">Add Person</a>
	<a href="/add-organization">Add Organization</a><br>
    </div>
         <div class="search">
            <h2>Search People</h2>
            <form action="/search-people" method="GET">
                <input type="text" name="q" placeholder="Search by name">
                <button type="submit">Search</button>
            </form>
        </div>
        <div class="search">
            <h2>Search Organizations</h2>
            <form action="/search-organizations" method="GET">
                <input type="text" name="q" placeholder="Search by name">
                <button type="submit">Search</button>
            </form>
        </div>
</body>
</html>

